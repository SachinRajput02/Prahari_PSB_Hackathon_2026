# Render Blueprint — New + → "Blueprint" → pick this repo, deploy.
services:
  - type: web
    name: prahari
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r ../requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
